services:
  prism:
    build: .
    ports:
      - "${PORT:-3080}:3080"
    volumes:
      - prism-data:/data
    env_file:
      - .env
    environment:
      - DB_PATH=/data/prism.db
      - HOST=0.0.0.0
    restart: unless-stopped

volumes:
  prism-data:
